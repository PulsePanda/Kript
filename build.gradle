apply plugin: 'java'

version = '1.2'

task fatJar(type: Jar) {
	manifest {
		attributes 'Implementation-Title': 'Kript', 'Implementation-Version': version
	}
	baseName = project.name
	from {configurations.compile.collect {it.isDirectory() ? it : zipTree(it)}}
	with jar
}
